# packages/types

TypeScript API client + types, generated from FastAPI's OpenAPI schema (via
`openapi-typescript`). Keeps `apps/web` in sync with `apps/api` without hand-written types.
Generated in Phase 2 once the API endpoints exist.
